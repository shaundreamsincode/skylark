<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Skylark - Sign Up</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Akshar:wght@300;700&family=Work+Sans:ital,wght@0,100;0,400;0,900&display=swap" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white font-sans min-h-screen flex flex-col">
  
  <!-- Header -->
  <header class="flex justify-between items-center p-6 bg-gray-800 shadow-md">
    <h1 class="text-3xl font-bold">
      <a href="/" class="hover:text-blue-400 transition-colors">Skylark</a>
    </h1>
  </header>

  <!-- Main Content -->
  <main class="flex-1 flex justify-center items-center py-12">
    <div class="bg-gray-800 rounded-lg shadow-lg p-8 w-full max-w-md">
      <h2 class="text-2xl font-bold text-center mb-6">Create Your Account</h2>
      
      <!-- Error Messages -->
      <% if @user.errors.any? %>
        <div class="bg-red-900 border border-red-700 text-red-200 px-4 py-3 rounded mb-4">
          <h3 class="font-bold">Please fix the following errors:</h3>
          <ul class="list-disc list-inside text-sm mt-2">
            <% @user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      
      <!-- Sign Up Form -->
      <%= form_with model: @user, url: users_path, method: :post, class: "space-y-4" do |f| %>
        
        <div>
          <%= f.label :first_name, "First Name", class: "block text-sm font-medium text-gray-400 mb-1" %>
          <%= f.text_field :first_name, class: "w-full px-4 py-2 rounded bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500", required: true %>
        </div>
        
        <div>
          <%= f.label :last_name, "Last Name", class: "block text-sm font-medium text-gray-400 mb-1" %>
          <%= f.text_field :last_name, class: "w-full px-4 py-2 rounded bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500", required: true %>
        </div>
        
        <div>
          <%= f.label :email, "Email", class: "block text-sm font-medium text-gray-400 mb-1" %>
          <%= f.email_field :email, class: "w-full px-4 py-2 rounded bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500", required: true %>
        </div>
        
        <div>
          <%= f.label :password, "Password", class: "block text-sm font-medium text-gray-400 mb-1" %>
          <%= f.password_field :password, class: "w-full px-4 py-2 rounded bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500", required: true %>
          <p class="text-xs text-gray-500 mt-1">Minimum 8 characters</p>
        </div>
        
        <div>
          <%= f.label :password_confirmation, "Confirm Password", class: "block text-sm font-medium text-gray-400 mb-1" %>
          <%= f.password_field :password_confirmation, class: "w-full px-4 py-2 rounded bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500", required: true %>
        </div>
        
        <div>
          <%= f.label :bio, "Bio (Optional)", class: "block text-sm font-medium text-gray-400 mb-1" %>
          <%= f.text_area :bio, rows: 3, class: "w-full px-4 py-2 rounded bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500", placeholder: "Tell us a bit about yourself..." %>
        </div>
        
        <%= f.submit "Create Account", class: "w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 rounded transition duration-200 cursor-pointer" %>
        
      <% end %>
      
      <!-- Sign In Link -->
      <div class="mt-6 text-center">
        <p class="text-gray-400">Already have an account?</p>
        <a href="/login" class="text-blue-400 hover:text-blue-300 underline">Sign in here</a>
      </div>
    </div>
  </main>

  <!-- Footer -->
  <footer class="text-center py-6 bg-gray-900 text-gray-500">
    <p>&copy; 2025 Skylark. All rights reserved.</p>
  </footer>

</body>
</html>
